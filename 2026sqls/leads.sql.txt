

CREATE TABLE source_table (
    id INT,
    start_tms DATE,
    end_tms DATE
);

INSERT INTO source_table (id, start_tms, end_tms) VALUES
(1, '2025-01-01', NULL),
(2, '2025-02-01', NULL),
(2, '2025-03-01', NULL),
(2, '2025-04-01', NULL),
(2, '2025-05-01', NULL);

-- select id, start_tms,lead(start_tms,1) over (order by id) from source_table;

select id, start_tms,lead(start_tms,1) over (partition by id order by start_tms) from source_table;
